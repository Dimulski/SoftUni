// Write a program that takes an input in the form of a string and transforms each of its characters at even position to its uppercase
// representation. Note that the string indexation is zero-based (of course, we are programmers) and the number zero is even.
// unfinished
using System;

namespace Uppercase_Chars_at_Even_Positions
{
    class Program
    {
        /*static void Main()
        {
            string input = Console.ReadLine();
            UppercahseCharsAtEvenPositions(input);
        }

        static string UppercahseCharsAtEvenPositions(string input)
        {
            char[] inputArray = input.ToCharArray();
            for (int i = 0; i < inputArray.Length; i++)
			{
                if (i % 2 == 0)
                {
                    inputArray[i].ToString().ToUpper();
                }
                Console.Write(inputArray[i]);
			}
            for (int i = 0; i < inputArray.Length; i++)
            {
                Console.Write(inputArray[i]);
            }
            return new string (inputArray);
        } */
        static void Main()
        {

        }
    }
}
